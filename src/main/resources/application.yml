server:
    servlet:
      context-path: /appDSDExpConsApi
      session:
        timeout: 6000

spring: 
  profiles:
      active: '@ACTIVED_PROPERTIES@'
  banner:
    location: classpath:/conf/banner.txt
      
    
logging:
  config: classpath:logback.xml 
    